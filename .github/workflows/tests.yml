name: Tests

on:
  push:
    branches: ["main", "feature/**"]
  pull_request:
    branches: ["main"]

jobs:
  test:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore SistemaVentas.Tests/SistemaVentas.Tests.csproj /p:EnableWindowsTargeting=true

      - name: Build
        run: dotnet build SistemaVentas.Tests/SistemaVentas.Tests.csproj --configuration Release --no-restore /p:EnableWindowsTargeting=true

      - name: Test with coverage
        run: dotnet test SistemaVentas.Tests/SistemaVentas.Tests.csproj --configuration Release --no-build /p:EnableWindowsTargeting=true --collect:"XPlat Code Coverage"
