apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "sistema-ventas.fullname" . }}
  labels:
    {{- include "sistema-ventas.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "sistema-ventas.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "sistema-ventas.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 5000
              protocol: TCP
          env:
            {{- range $key, $val := .Values.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
            - name: Security__JwtSecretKey
              valueFrom:
                secretKeyRef:
                  name: {{ include "sistema-ventas.fullname" . }}-secrets
                  key: jwt-secret
            - name: ConnectionStrings__Default
              valueFrom:
                secretKeyRef:
                  name: {{ include "sistema-ventas.fullname" . }}-secrets
                  key: connection-string
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
