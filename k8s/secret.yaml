# NOTE: This is a template. Do NOT store real secrets here.
# Use kubectl create secret or a secrets manager in production.
apiVersion: v1
kind: Secret
metadata:
  name: sistema-ventas-secrets
  namespace: sistema-ventas
type: Opaque
stringData:
  jwt-secret: "REPLACE_WITH_REAL_SECRET_MIN_32_CHARS"
  connection-string: "Data Source=/data/ventas.db"
