apiVersion: v1
kind: Service
metadata:
  name: sistema-ventas-api
  namespace: sistema-ventas
  labels:
    app: sistema-ventas-api
spec:
  selector:
    app: sistema-ventas-api
  ports:
  - name: http
    port: 80
    targetPort: 5000
    protocol: TCP
  type: ClusterIP
